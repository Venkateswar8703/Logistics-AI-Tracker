# Logistics AI Tracker — Environment Variables
# Copy this file to .env and fill in the values.

# Optional: Set to enable GPT-powered chat responses.
# Leave blank to use the built-in rule-based responder (no API key needed).
OPENAI_API_KEY=

# Set to true for verbose logging and debug mode.
DEBUG=true
